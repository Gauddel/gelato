type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  executor: Bytes! # address
  taskReceiptId: BigInt! # uint256
}

type Provider @entity {
  id: ID!
  users : [Bytes!]
}

type Executor @entity {
  id: ID!
  providers : [Bytes!]
}